import { Routes } from '@angular/router';
import { EventsComponent } from './events.component'; // Убедитесь, что путь относительный и правильный

export const routes: Routes = [
  { path: '', component: EventsComponent }
];
